@model IEnumerable<WebApplication3.Models.Game>

@{
    ViewData["Title"] = "Trend";
}

<h1>Trend</h1>
<br />
<br />

<div class="container-fluid">
    <div class="row">
        <h3>Field Goals</h3>
        <canvas id="Graph1">
        </canvas>
    </div>
</div>
<br />
<br />
<div class="row">
    <div class="col-md">
        <canvas id="Graph2">
        </canvas>
    </div>
</div>
<div class="row">
    <div class="col-md">
        <canvas id="Graph3">
        </canvas>
    </div>
</div>

<script type="text/javascript">

    function GetJSON_Simple() {
        var resp2 = [];
        $.ajax({
            type: "GET",
            url: '/Games/Method',
            async: false,
            contentType: "application/json",
            success: function (data) {
                resp2.push(data);
            },
            error: function (req, status, error) {
                alert("error");
            }

        });
        return resp2;
    }

 

 //    var simpleData = GetJSON_Simple();

 //   var ctx = document.getElementById("Graph1")

  //  Chart.defaults.global.elements.rectangle.backgroundColor = myBorderColors;

   /* Chart.defaults.global.elements.rectangle.borderColor = "rgba(0, 177, 106, 1)";
    Chart.defaults.global.elements.rectangle.borderWidth = 5;
    Chart.defaults.global.responsive = true;
    Chart.defaults.scale.ticks.beginAtZero = "true";
   */
    /*

    var barChartData = {
        labels: simpleData[0].myNotes,
        datasets: [{
            label: 'FGM',
            data: simpleData[0].myFGM
        }
        ]
    };

    window.onload = function () {
        window.Graph1 = Chart.Bar(ctx, { data: barChartData });

    };

    var win = [true, false, false, true, true, true, false, false]; //simpleData[0].myWin;
    var myBorderColors = win.map(b => b ? "rgba(0, 177, 106, 1)" : "rgba(207, 0, 15, 1)");
    Chart.defaults.global.datasets.bar.borderColor = myBorderColors;


    $.each(win, function (index, value) {
        if (value === true) {
            myBorderColors[index] = "rgba(0, 177, 106, 1)";
        } else {

            myBorderColors[index] = "rgba(207, 0, 15, 1)";
        }
    }
     
    );


    //Chart.defaults.global.datasets.bar.borderColor = myBorderColors;
    
    console.log(myBorderColors);


    console.log(Chart.defaults);
    */


    console.log(Chart.defaults);

    var win = [true, false, false, true];
var myBorderColors = win.map(b => b ? "rgba(0, 177, 106, 1)" : "rgba(207, 0, 15, 1)");

    Chart.defaults.global.elements.rectangle.backgroundColor = myBorderColors;
  //  Chart.defaults.global.datasets.bar.borderColor = myBorderColors;

new Chart(document.getElementById('Graph3'), {
  type: 'bar',
  data: {
    labels: ['A', 'B', 'C', 'D'],
    datasets: [{
      label: "My Dataset",
      data: [3, 5, 4, 2],
      borderWidth: 3
    }]
  },
  options: {
    legend: {
      display: false
    },
    scales: {
      yAxes: [{
        ticks: {
          beginAtZero: true
        }
      }]
    }
  }
});



    </script>